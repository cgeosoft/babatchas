<section class="section-sm">
    <div class="container">
        <div class="row">
            <div class="col-md-8 col-md-offset-2">
                {{#each description}}
                <p>{{this}}</p>
                {{/each}}
            </div>
        </div>
    </div>
</section>