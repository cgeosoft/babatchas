{{>_start}}
<main class="main main-full">
    <div class="container-fluid">
        <div class="opener">
            <div class="hr hr-top"></div>
            <img class="img-responsive" style="margin: 0 auto;max-width: 800px;padding-left: 9%;" src="/img/logo.png">
            <div class="hr hr-bottom"></div>
            <div class="awards">
                {{#each awards}}
                <a href="{{url}}" target="_blank" class="award">
                    <img src="/img/awards/{{img}}" alt="">
                    <div>{{{label}}}</div>
                </a>
                {{/each}}
            </div>
        </div>
    </div>
</main>
{{>_end}}